class Tap {
  constructor(id, name, waitingSetup, setup, volume) {
    this.id = id;
    this.name = name;
    this.waitingSetup = waitingSetup;
    this.setup = setup;
    this.volume = volume;
  }
}

export default Tap;
